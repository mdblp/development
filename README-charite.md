Before running the docker stack make sure to download the custom images for Blip and Hydrophone and import into docker:

`docker load < hydrophone-charite.tar.gz`  
`docker load < blip-charite.tar.gz`